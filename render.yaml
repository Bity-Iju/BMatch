services:
  - type: web
    name: bmatch
    runtime: node
    plan: free
    buildCommand: echo "No build required"
    startCommand: node web/server.js
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false
